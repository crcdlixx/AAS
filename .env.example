# docker-compose will auto-load `.env` in this folder.
# Copy this file to `.env` and update values.

DOCKERHUB_USERNAME=your-dockerhub-namespace
IMAGE_TAG=latest
WEB_PORT=8080

# Production (HTTPS via Caddy)
DOMAIN=example.com
HTTP_PORT=80
HTTPS_PORT=443

# Optional
PYODIDE_LOG=off

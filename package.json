{
  "name": "ai-question-solver",
  "version": "1.0.0",
  "description": "AI搜题网页应用",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev:mcp": "cd tools/mcp_python && npm run build && npm start",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install && cd ../tools/mcp_python && npm install"
  },
  "keywords": ["ai", "question-solver", "ocr"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
